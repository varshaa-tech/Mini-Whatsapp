<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Neha & Shradha Chat</title>
  <style>
    * {
      box-sizing: border-box;
    }

    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #121212;
      color: #eee;
    }

    /* 3D animated background */
    .background-3d {
      position: relative;
      width: 100%;
      min-height: 100vh;
      overflow: hidden;
      background: linear-gradient(120deg, #1f1c2c, #928dab);
      display: flex;
      justify-content: center;
      align-items: center;
      perspective: 1000px;
      padding: 30px 10px;
    }

    .background-3d::before,
    .background-3d::after {
      content: "";
      position: absolute;
      width: 200%;
      height: 200%;
      top: -50%;
      left: -50%;
      background: repeating-linear-gradient(
          45deg,
          rgba(255, 255, 255, 0.05) 0 1px,
          transparent 1px 20px
        ),
        repeating-linear-gradient(
          -45deg,
          rgba(255, 255, 255, 0.05) 0 1px,
          transparent 1px 20px
        );
      animation: rotateGrid 30s linear infinite;
      transform-style: preserve-3d;
      z-index: 0;
    }

    .background-3d::after {
      animation-direction: reverse;
      opacity: 0.3;
    }

    @keyframes rotateGrid {
      0% {
        transform: rotateX(0deg) rotateY(0deg);
      }

      100% {
        transform: rotateX(360deg) rotateY(360deg);
      }
    }

    .chat-container {
      max-width: 700px;
      width: 100%;
      background-color: #1e1e2fdd;
      border-radius: 10px;
      padding: 20px;
      box-shadow: 0 0 20px #00000088;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      animation: slideFadeIn 0.6s ease;
      position: relative;
      z-index: 1;
      backdrop-filter: blur(8px);
    }

    @keyframes slideFadeIn {
      0% {
        transform: scale(0.95) translateY(30px);
        opacity: 0;
      }

      100% {
        transform: scale(1) translateY(0);
        opacity: 1;
      }
    }

    .messages {
      flex: 1;
      max-height: 70vh;
      overflow-y: auto;
      margin-bottom: 20px;
      padding-right: 10px;
    }

    .messages::-webkit-scrollbar {
      width: 6px;
    }

    .messages::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.2);
      border-radius: 4px;
    }

    .message {
      margin-bottom: 15px;
      padding: 10px 15px;
      border-radius: 15px;
      max-width: 75%;
      clear: both;
      word-wrap: break-word;
      position: relative;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      animation: popIn 0.5s ease;
      transform-origin: top center;
      perspective: 800px;
    }

    @keyframes popIn {
      0% {
        transform: rotateX(-90deg);
        opacity: 0;
      }

      100% {
        transform: rotateX(0);
        opacity: 1;
      }
    }

    .message:hover {
      transform: scale(1.02) rotateY(2deg);
      box-shadow: 0 4px 15px rgba(255, 255, 255, 0.2);
    }

    .left {
      background-color: #2e2e3f;
      float: left;
      border: 1px solid #444;
      color: #ccc;
      text-align: left;
    }

    .right {
      background-color: #3ddc84;
      float: right;
      color: #000;
      text-align: right;
    }

    .sender {
      font-weight: bold;
      font-size: 0.85em;
      margin-bottom: 5px;
      color: #888;
    }

    .timestamp {
      display: block;
      font-size: 0.75em;
      margin-top: 5px;
      color: #aaa;
    }

    .edit-link {
      display: block;
      font-size: 0.75em;
      margin-top: 5px;
      color: #eff0f1;
      text-decoration: underline;
    }

    form {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      align-items: center;
    }

    select,
    input[type="text"] {
      flex: 1 1 100px;
      padding: 10px;
      font-size: 1em;
      border: 1px solid #555;
      border-radius: 5px;
      background: #222;
      color: #eee;
    }

    button {
      padding: 10px 16px;
      font-size: 1em;
      background-color: #00c853;
      border: none;
      color: white;
      border-radius: 5px;
      cursor: pointer;
    }

    button:hover {
      background-color: #009624;
    }

    @media (max-width: 600px) {
      form {
        flex-direction: column;
        align-items: stretch;
      }

      select,
      input[type="text"],
      button {
        width: 100%;
      }

      .message {
        max-width: 100%;
      }
    }
    
    .message-actions {
      display: flex;
      justify-content: space-between;
      margin-top: 8px;
    }

   

    .delete-btn {
      background: none;
      border: none;
      color: #ff6666;
      font-size: 0.75em;
      cursor: pointer;
      text-decoration: underline;
    }

    .delete-btn:hover {
      color: red;
    }

    /* Form select box update */
    select {
      appearance: none;
      background-image: url("data:image/svg+xml;charset=US-ASCII,%3Csvg%20fill%3D'%23eee'%20height%3D'24'%20viewBox%3D'0%200%2024%2024'%20width%3D'24'%20xmlns%3D'http://www.w3.org/2000/svg'%3E%3Cpath%20d%3D'M7%2010l5%205%205-5z'/%3E%3C/svg%3E");
      background-repeat: no-repeat;
      background-position: right 10px center;
      background-size: 16px 16px;
      padding-right: 30px;
    }
  </style>
</head>

<body>
  <div class="background-3d">
    <div class="chat-container">
      <div class="messages">
        <h2 style="text-align:center; color:#66ffcc;position: sticky;top: 20px;z-index: 2;">Chat: Neha ↔ Shradha</h2>

        <% chats.forEach(chat => { %>
          <div class="message <%= chat.to.toLowerCase() === 'neha' ? 'left' : 'right' %>">
            <div class="sender">
              <%= chat.from %> → <%= chat.to %>
            </div>
            <div><%= chat.msg %></div>
            <span class="timestamp"><%= new Date(chat.created_at).toLocaleString() %></span>

            <div class="message-actions">
              <a class="edit-link" href="/chats/<%= chat._id %>/edit">Edit</a>
              <form action="/chats/<%= chat._id %>?_method=DELETE" method="POST" style="display:inline;">
                <button type="submit" class="delete-btn" onclick="return confirm('Delete this message?')">Delete</button>
              </form>
            </div>
          </div>
        <% }) %>
      </div>

      <!-- Chat Input Form -->
      <form action="/chats" method="POST">
        <select name="from" required>
          <option value="" disabled selected>Sender</option>
          <option value="neha">Neha</option>
          <option value="shradha">Shradha</option>
        </select>

        <select name="to" required>
          <option value="" disabled selected>Receiver</option>
          <option value="neha">Neha</option>
          <option value="shradha">Shradha</option>
        </select>

        <input type="text" name="msg" placeholder="Type a message..." required />
        <button type="submit">Send</button>
      </form>
    </div>
  </div>
</body>

</html>
